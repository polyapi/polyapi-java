export enum JobType {
    PERIODICALLY = 'periodically',
    INTERVAL = 'interval',
    ON_TIME = 'on_time'
}


export enum JobExecutionType {
    SECUENTIAL = 'secuential',
    PARALLEL = 'parallel'
}